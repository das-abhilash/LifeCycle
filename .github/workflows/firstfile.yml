name: CI notify

# Only trigger, when the build workflow succeeded
on:
  workflow_run:
    workflows: ["Labeler"]
    types:
      - completed

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:

      - name: Notify Slack and send eMail
        run: |
          echo "ci notify"
